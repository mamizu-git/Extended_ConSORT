init_x(n, x, p) 
[ <#n: int, x: int, p: int ref> -> 
  <#n: int, x: int, p: int ref | int> ]
{
  if n <= 0 then {
    1
  } else {
    p := x;
    if n = 1 then {
      1
    } else {
      let q = p + 1 in
      let m = n - 1 in
      let d = init_x(m, x, q) in
      alias(q = p + 1);
      1
    }
  }
}

{
  let p = mkarray 3 in
  let m = 3 in
  let x = 1 in
  let d = init_x(m, x, p) in
  let pp = *p in
  assert(pp = 1);
  let q = p + 1 in
  let qq = *q in
  assert(qq = 1);
  let r = q + 1 in
  let rr = *r in
  assert(rr = 1);
  ()
}
